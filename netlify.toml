[build]
  command = "npm run build:netlify"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--legacy-peer-deps"

# Next.js API routes + auth need the Node runtime bundle
[functions]
  node_bundler = "esbuild"
